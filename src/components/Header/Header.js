import React from "react";
import "./Header.css";
import Logo from "../Logo/Logo";

function Header() {
  return (
    <header className="Header">
      <Logo />
    </header>
  );
}

export default Header;
